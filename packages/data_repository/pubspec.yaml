name: data_repository
description: Dart package which manages the local and cloud database domain.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  equatable: ^2.0.5
  cloud_firestore:
  firebase_core: ^2.12.0
  flutter:
    sdk: flutter
  meta: ^1.8.0

dev_dependencies:
  firebase_core_platform_interface: ^4.8.0
  flutter_test:
    sdk: flutter
  mocktail: ^0.3.0
  plugin_platform_interface: ^2.1.4
